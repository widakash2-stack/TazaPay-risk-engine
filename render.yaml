services:
  - type: web
    name: tazapay-risk-engine
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn api:app --workers 1 --timeout 120
    envVars:
      - key: ANTHROPIC_API_KEY
        sync: false   # set manually in Render dashboard
    rootDir: backend
